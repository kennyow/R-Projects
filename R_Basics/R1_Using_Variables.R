A <- 10
B <- 5

C <- A + B #Spaces don't matter

#Print Out the result:
C

#You can select more than 1 variable to execute at the same time
var1 <- 2.5
var2 <- 4

result <- var1 / var2
result

answer <-sqrt(var2)
answer

#Print the message (it already includes the space between the two words)
greeting <-"Hello"
name <- "Bob"
message <- paste(greeting, name)
message
